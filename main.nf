params.reads = 

/*
 * Step 1. Print each record
 */
process printSequences {
 
    input:
    
 
    output:
    
 
    """
    
    """
 
}

/*
 * print the channel content
 */
result.subscribe { println it }